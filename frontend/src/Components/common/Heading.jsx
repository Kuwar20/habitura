import React from 'react'

const Heading = ({title}) => {
  return (
    <div className="text-4xl font-bold font-primary text-secondary text-center mb-8">
      {title}
    </div>
  )
}

export default Heading
